@model EditComisionViewModel

    <h2 class="mb-3 text-center">Editar comisión</h2>

<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="alert alert-danger alert-dismissible fade show">
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
            <input asp-for="Comision.ID" type="hidden" />
            <div class="mb-3">
                <label asp-for="Comision.Descripcion" class="form-label">Descripcion</label>
                <input maxlength="20" minlength="3" asp-for="Comision.Descripcion" class="form-control" placeholder="@Model.Comision.Descripcion" value="@Model.Comision.Descripcion" />
                <span asp-validation-for="Comision.Descripcion" class="text-danger"></span>
            </div>
            <div class="mb-3">
                <label asp-for="Comision.AnoEspecialidad" class="form-label">Año especialidad</label>
                <input maxlength="20" minlength="3" asp-for="Comision.AnoEspecialidad" class="form-control" placeholder="@Model.Comision.AnoEspecialidad" value="@Model.Comision.AnoEspecialidad" />
                <span asp-validation-for="Comision.AnoEspecialidad" class="text-danger"></span>
            </div>
            <select class="form-select mb-3" asp-for="Comision.IDPlan" asp-items="Model.Planes"></select>
            <div class="mb-3">
                <button type="submit" class="btn btn-primary">Guardar</button>
                <a class="btn btn-danger" asp-action="Index">Cancelar</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}